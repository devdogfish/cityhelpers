title: City Helpers — Team Notes
url: https://devdogfish.github.io
baseurl: /cityhelpers
markdown: kramdown
exclude: [README.md, sync-notes.py, publish.sh, verify-published.py]
defaults:
  - scope:
      path: ""
    values:
      layout: default
